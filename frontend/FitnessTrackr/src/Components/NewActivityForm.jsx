const NewActivityForm = ({closeModal}) => {
  return (
    <div>
      <p>I'm where the stuff goes!</p>
      <button onClick={closeModal}>Bye</button>
    </div>
  )
}

export default NewActivityForm